name: ci

# 푸시/PR마다 스냅샷 리니지 + 골든 케이스 + 프로덕션 빌드 검증
on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  golden-and-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - name: Data snapshot lineage check (manifest sha256)
        run: npm run snapshot:check
      - name: Golden case regression (fail = block)
        run: npm run golden
      - name: Production build
        run: npm run build
